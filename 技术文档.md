# 日知录记账 — 技术文档

> 最后更新：2026-08（v1.5.6，云函数中转架构）
> 阅读对象：后续维护本项目的开发者 / 使用者

---

## 1. 项目概述

日知录记账是一个**个人记账 + 心情/天气/纪录** 的单页应用（SPA），核心特点：

- **云函数中转**：前端 → CloudBase HTTP 访问服务 → 云函数 → 数据库（国内节点，大陆快）
- **手机/电脑数据共享**：同一份云端数据，任意设备打开即用
- **免费部署**：托管在 GitHub Pages，数据库在腾讯云（上海），大陆访问快
- **离线友好**：localStorage 缓存，秒开，网络抖动自动重试

### 核心文件

| 文件 | 作用 |
|---|---|
| `index.html` | **全部前端代码**（HTML + CSS + JS 单文件，约 150KB / 2900 行） |
| `app.py` | 遗留的 Flask 后端（仅本地调试用，生产环境不使用） |
| `data.db` | 遗留的本地 SQLite 数据库（已迁移到云端） |
| `迁移到Turso.py` | SQLite → Turso 的旧迁移脚本（历史遗留） |
| `cloudfunctions/rizhilu-api/` | **数据 API 云函数**（Node，部署在腾讯云 CloudBase） |
| `cloudbaserc.json` | CloudBase CLI 部署配置 |
| `技术文档.md` | 本文档 |

---

## 2. 系统架构

```
┌─────────────┐   ┌─────────────┐
│  手机浏览器   │   │  电脑浏览器   │
└──────┬──────┘   └──────┬──────┘
       │  HTTPS (GitHub Pages 托管 index.html)
       ▼                 ▼
┌──────────────────────────────────┐
│      index.html（单文件前端）       │
│  ┌────────────────────────────┐  │
│  │  api() / tursoPipeline()    │  │   ← 数据访问层
│  └──────────────┬─────────────┘  │
│  localStorage 缓存（smartLoad）   │
└─────────────────┬────────────────┘
                  │ HTTPS fetch（带 CORS）
                  ▼
        ┌───────────────────────────────────┐
        │ CloudBase HTTP 访问服务（默认域名）    │
        │ https://<env>.service.tcloudbase.com │
        └───────────────┬───────────────────┘
                  │ 触发云函数
                  ▼
        ┌────────────────────┐
        │ 云函数 rizhilu-api  │  ← Node，内部访问数据库（无跨域问题）
        └───────────────┬────┘
                  ▼
        ┌────────────────────────┐
        │ CloudBase 文档型数据库   │  ← 上海节点
        │ 环境: rizhilu-d2g8bc0qg3a96ace6 │
        └────────────────────────┘
```

**关键点**：浏览器直接访问 Turso 的 HTTP API，没有自己的服务器。`app.py` 已不参与生产流程。

---

## 3. 技术栈

| 层 | 技术 |
|---|---|
| 前端 | 原生 HTML + CSS + JavaScript（无框架、无构建） |
| 数据库 | 腾讯云 CloudBase（文档型 NoSQL，上海节点） |
| 中间层 | CloudBase 云函数（Node.js，数据 API 中转） |
| 存储 | localStorage（前端缓存） |
| 托管 | GitHub Pages |
| 版本管理 | Git（GitHub 仓库 `chensiyue1112/rizhilu`） |

---

## 4. 数据存储 — Turso

### 4.1 连接信息

```javascript
// index.html 数据层顶部
const CB_API_BASE = 'https://rizhilu-d2g8bc0qg3a96ace6.service.tcloudbase.com/cloudfunctions/rizhilu-api';
```

浏览器直接 fetch 这个地址（GET/POST/PUT/DELETE），由云函数中转访问数据库。

> ⚠️ **为什么不用 Web SDK 直连**：CloudBase 免费开发版不支持「Web 安全域名」，
> CORS 不返回 `Access-Control-Allow-Credentials`，新版 SDK 的 withCredentials 跨域请求
> 被浏览器拦截（`Failed to fetch`）；旧版 SDK 协议又过旧。**云函数中转绕开了所有 CORS 限制**
> （云函数在云端访问数据库，返回头带 `Access-Control-Allow-Origin: *`）。

### 4.2 表结构（TABLES_META，index.html 第 1177 行）

```javascript
income:           date, amount, category, note, large   // large=1 表示大额收入（如工资，不计入概览图）
expense:          date, amount, category, description, large   // large=1 表示大额支出
finance_snapshot: date, liquid_assets, debt, stock, fund, convertible_bond
electricity:      date, reading
mood:             date, rating, note                             // rating 1-5
weather:          date, weather, note
record:           date, content, done, due_date, followup        // done=1 表示已过去
```

- `large` 列：把大额支出（如房租、电动车）从日常支出中剔除，单独统计，避免图表被拉爆
- `numCols` 标记数字列，导入/导出时用于类型转换

### 4.3 时区处理

所有日期用**本地时区**字符串 `YYYY-MM-DD` 存储，前端提供三个工具函数：

```javascript
todayStr()      // 今天：2026-08-07
thisMonthStr()  // 本月：2026-08
nowStr()        // 带时间：2026-08-07 12:30:45
```

**不要用 `toISOString()`**（UTC 时区会导致北京午夜后日期错一天）——这是历史上修过的一个 bug。

---

## 5. 前端数据访问层

### 5.1 数据访问层（index.html 数据层顶部）

```javascript
const CB_API_BASE = 'https://rizhilu-d2g8bc0qg3a96ace6.service.tcloudbase.com/cloudfunctions/rizhilu-api';
async function api(url, opts) { ... }  // fetch 云函数，20s 超时 + 5 次重试
```

前端所有数据操作都走 `api('/api/xxx')`，内部把 `/api` 前缀替换成 `CB_API_BASE`：

| 前端 URL | 云函数处理 |
|---|---|
| `GET /api/income?month=` | 列表（可按月过滤） |
| `POST /api/income` | 新增 |
| `PUT /api/income/{id}` | 更新 |
| `DELETE /api/income/{id}` | 删除 |
| `GET /api/stats/overview?month=` | 概览统计 |
| `GET /api/stats/expense\|income` | 分类汇总 |
| `GET /api/stats/trend` | 近 6 月收支趋势 |
| `GET /api/export` / `POST /api/import` | 导出 / 覆盖式导入 |

**云函数代码在 `cloudfunctions/rizhilu-api/index.js`**（Node，用 @cloudbase/node-sdk 访问数据库）。
**要点**：
- 云函数返回头统一带 `Access-Control-Allow-Origin: *`
- 必须处理 **OPTIONS 预检**（返回 204 + CORS 头），否则浏览器跨域 POST/PUT/DELETE 全部 `Failed to fetch`
- 查询结果映射 `{ ...doc, id: doc._id }`，前端 `r.id` 用法不变

### 5.2 api(url, opts) — 逻辑层（index.html 第 1211 行）

把 REST 风格的 URL 翻译成 Turso SQL，内置：

| URL | 行为 |
|---|---|
| `GET /api/income`、`/api/expense` 等 | 全表查询（可按 `?month=2026-07` 过滤） |
| `POST /api/income` 等 | 插入 |
| `PUT /api/income/{id}` | 更新 |
| `DELETE /api/income/{id}` | 删除 |
| `GET /api/stats/overview?month=...` | 概览统计（收入/日常支出/大额支出/结余 + 最新财务快照 + 最新电表） |
| `GET /api/stats/expense|income` | 分类汇总（饼图数据） |
| `GET /api/export` | 导出全部数据为 JSON（Blob） |
| `POST /api/import` | 导入（merge 合并 / replace 清空重建） |

### 5.3 缓存系统 — smartLoad（index.html 第 1410 行附近）

```javascript
const CACHE_KEY = 'rizhilu_cache_v1';           // localStorage 键
const CACHE_ALLOWED = ['expenses','incomes','finances','electricity','overview','stats','moods','weather','records'];

function smartLoad(key, loader, renderer) {
    try { if (apiCache[key]) renderer(apiCache[key]); }   // 先渲染缓存（秒开）
    catch(e) { delete apiCache[key]; saveCache(); }        // 缓存损坏 → 删除自愈
    return loader().then(data => {                         // 后台刷新
        apiCache[key] = data; saveCache(); renderer(data);
    }).catch(e => { if (!apiCache[key]) throw e; });
}
```

- 概览/统计的缓存 key 带月份（如 `overview-2026-07`），切换月份互不干扰
- `preloadMoreTabs()`：页面打开 1.2 秒后**后台预加载**低频页面（财务/心情/天气/纪录），
  点开"更多"分支时秒开，不卡顿

---

## 6. 页面功能模块

| Tab | 功能 | 关键函数 |
|---|---|---|
| 概览 | 月份切换 ◀▶、4 张卡片、收入/支出柱状图（可切换） | `loadOverview`、`drawOverviewChart` |
| 支出 | 新增/编辑/删除、分类下拉（自定义控件）、大额勾选（仅编辑时） | `loadExpenses`、`saveExpense` |
| 收入 | 同上（无大额） | `loadIncomes`、`saveIncome` |
| 纪录 | 记录"未来要发生的事"，标记已过去，补充感受 | `loadRecords`、`saveRecord` |
| 财务 | 每月资产快照（现金/负债/股票/基金/可转债） | `loadFinances`、`drawFinanceChart` |
| 用电 | 电表读数记录 | `loadElectricities` |
| 心情 | 月历 + 5 档表情，当天可点选，其他天需编辑 | `loadMoods`、`pickMood` |
| 天气 | 月历 + 晴/多云/阴/雨 | `loadWeathers` |
| 统计 | 分类环形图 + 收支趋势图 + 排行 | `loadStats`、`drawDonutChart`、`drawTrendChart` |

### 6.1 自定义控件（重要！）

**背景**：小米自带浏览器对原生 `<select>` 和 `<input type="date">` 强制渲染，
CSS 无法控制箭头位置/样式（历史多个版本反复修不好，最终彻底重写）。

**方案**：全部换成纯 div 自定义控件，样式 100% 可控：

- **分类下拉**（`.custom-select`）：点击弹选项列表，SVG 箭头紧贴文字
- **日期选择**（`.custom-date`）：点击弹**最近 7 天快捷列表**（8月7日 / 8月6日…），
  显示用月日（`08-07`），存储用完整日期（`2026-08-07`）
- 原控件 id 保留为 `<input type="hidden">`，JS 读写逻辑零改动：
  `$('exp_category').value` 依然有效

### 6.2 图表

Canvas 手绘，无第三方库：
- 柱状图：鼠标/触摸悬停显示数值 + 贯穿虚线（`_hot` 热区，切换页面时清理）
- 环形图、趋势图：统计页

### 6.3 大额支出逻辑

```sql
-- 概览"日常支出"：排除大额
SELECT SUM(amount) FROM expense WHERE date LIKE '2026-07%' AND large = 0
-- 概览"大额支出"：只算大额
SELECT SUM(amount) FROM expense WHERE date LIKE '2026-07%' AND large = 1
```

新增支出默认不显示大额勾选（避免打扰），**编辑已有记录时才显示**勾选框。

---

## 7. 移动端适配

- `@media (max-width: 768px)` 切换移动布局
- 底部导航 5 项：概览 / 支出 / 收入 / 纪录 / 更多
- "更多"弹出抽屉（财务 / 用电 / 心情 / 天气 / 统计）
- 收入/支出表单：第一行 日期|分类|金额，第二行 描述/备注+保存（底部对齐）
- 表格 `table-layout: fixed`，操作列 32%（编辑/删除按钮并排不裁剪）
- 点开支出/收入页自动聚焦金额框（弹键盘，延迟 350ms 等页面切换完成）
- 日期/分类控件宽度 `fit-content`（贴合内容），桌面端固定 110px/140px（与金额/备注对齐）

---

## 8. 部署流程（GitHub Pages）

```
git add index.html
git commit -m "v1.3.8 说明"
git push origin main
```

- 推送后 GitHub Actions 自动构建部署，**需等待约 1~5 分钟**
- 部署完成前线上是旧版；部署后 **CDN 缓存可能滞后**（尤其中国大陆访问），
  需强刷（Ctrl+F5 / 无痕）确认
- **每次改动必须升版本号**（见 §9），用户靠版本号确认是否拿到新版

### 版本号三处位置

1. 浏览器标签页标题：`日知录记账 v1.3.7`（JS 自动同步）
2. 电脑端：左侧栏底部
3. 手机端：点"更多"→ 抽屉底部

---

## 9. 版本管理规范

语义化版本 `v主.次.补丁`：

| 变更类型 | 版本示例 |
|---|---|
| 新功能 / 重构 | v1.3.0 → v1.4.0 |
| 小修复 | v1.3.0 → v1.3.1 |
| 破坏性变更 | v1.x → v2.0.0 |

**改版本号要改 3 处**：`appVersion`（桌面）、`appVersionM`（手机抽屉）、`<title>`（标签页）。

---

## 10. 数据迁移历史

1. **SQLite（本地 data.db）→ Turso**：`迁移到Turso.py`（历史，已废弃）
2. **Turso → CloudBase**（2026-08，一次性）：
   - python 从 Turso HTTP API 导出全部表 → `turso_dump.json`
   - Node + `@cloudbase/node-sdk` 建集合 + 写入
   - 数据量：income 331、expense 101、finance_snapshot 3、electricity 26、mood 8、weather 10、record 2
3. **架构定型：云函数中转**（v1.5.0+）：
   - 用 CloudBase CLI（`tcb`，密钥登录）部署云函数 + HTTP 路由
   - 部署命令：`tcb fn deploy rizhilu-api --dir cloudfunctions/rizhilu-api --force --install-dependency true`
   - 云函数代码、cloudbaserc.json 都在 git 仓库

---

## 11. 已知问题与注意事项

1. **API 地址公开**：CB_API_BASE 在前端，任何人可调用（数据可被读/写）。
   防护：CloudBase 控制台可对集合配安全规则；但免费版限制较多。个人记账可接受。
2. **免费额度**：CloudBase 按量计费，记账用量远低于免费额度（读 5 万次/月）。
   注意：**免费环境有 Web 安全域名功能限制**（这是当初不用 Web SDK 直连的原因）。
3. **localStorage 缓存**：缓存键固定（`rizhilu_cache_v1`），smartLoad 渲染失败自动删坏缓存自愈。
4. **`app.py` 已废弃**：仅本地调试用，不要依赖它做生产。
5. **云函数部署**：改 `cloudfunctions/rizhilu-api/index.js` 后需重新部署：
   `tcb fn deploy rizhilu-api --dir cloudfunctions/rizhilu-api --force --install-dependency true`
   （云函数即时生效，前端版本号仍会 bump 便于确认）
6. **历史坑记录**（遇到类似问题先查这些）：
   - 本地时区日期（禁 toISOString）
   - 整数可能是字符串（`Number(r.done) === 1` 判断）
   - 按钮 onclick 模板字符串用 `${r.id}` 而不是字符串拼接
   - 原生 select/date 在小米浏览器不可控（用自定义 div 控件）
   - **跨域写操作必须处理 OPTIONS 预检**（返回 204 + CORS 头）
   - **改 index.html 注意保留 `<script>` 标签、全局状态、工具函数**（历史上误删过导致页面全崩）
